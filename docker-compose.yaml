services:
  postgres:
    image: postgres:15
    container_name: minitrello-db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    env_file:
      - .env
    restart: always
volumes:
  pgdata: